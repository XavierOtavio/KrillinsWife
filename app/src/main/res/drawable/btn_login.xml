<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When the button is pressed, the background color will change to colorSecondaryDark -->
    <item android:state_pressed="true" android:drawable="@drawable/btn_corners_round" />
    <!-- When the button is not pressed, the background color will change to colorSecondary -->
    <item android:drawable="@drawable/btn_corners_round" />
</selector>